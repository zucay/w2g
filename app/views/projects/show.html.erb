<h5>HOME > <%= resource.genre.name %></h5>
<h2><%= resource.genre.name %>「<%= resource.name %>」</h2>
<% spots = resource.spots.active %>
<div id='content' style="width:690px">
<%= resource.description %>
<%= resource.created_at.strftime("(%Y/%m)") %>
<script type="text/javascript">
<!--
window.onload = function(){
  //alert('hoge');
  window.map = new MyMap("ymap");
	//window.map.drawJapan();
	window.map.drawHonshu();
	window.map.hideSearch();
	window.map.hideCenterMark();

	<% spots.each do |sp| %>
		window.map.addLinkMarker(<%= sp.lat_world %>,<%= sp.lng_world %>, "<%= spot_path(sp) %>", {title:"<%= sp.name %><br><img src=<%= sp.pic0.url(:small) %>><br><%= sp.about_body.first(10) + '…' %>"});
	<% end  %>

};

//-->
</script>

<ul class="champagne">
	<% spots.each_with_index do |sp, i|  %>
		<li>
			<%= link_to sp do  %>
				<% title = "#{sp.name.to_s}(#{sp.pref.to_s})"  %>
				<%= image_tag sp.pic0.url(:small), :class => "tip", :title => title %>
			<% end  %>
		</li>
	<% end  %>
</ul>
<div id="ymap" style="width:97%; height:480px"></div>
</div>
