<%= render 'header' %>
<%= semantic_form_for @spot, :html=>{:multipart => true} do |f| %>
  この度はご協力
  <%= f.inputs do %>
		<br />
		STEP1. ご回答いただく方のお名前をご記入くださいませ。
		<%= render :partial => 'caretaker', :locals =>{:f => f} %>
		<br />
		STEP2. 
		<%= render :partial => 'picform', :locals =>{:f => f} %>
	<% end  %>
	(次の画面が表示されるまでウィンドウは閉じないようお願いいたします)
	<%= f.buttons %>
<% end  %>
<%= render 'footer' %>
